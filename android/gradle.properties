# gradle.properties - PRODUCTION OPTIMIZED FOR FLUTTER & NOTIFICATIONS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🧠 JVM MEMORY SETTINGS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# CRITICAL: Mengatasi error "Insufficient system resources"
# Increase jika build error dengan OutOfMemoryError

org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseG1GC

# Penjelasan:
# -Xmx4096m          : Maximum heap size 4GB (sesuaikan dengan RAM)
# -XX:MaxMetaspaceSize=1024m : Metaspace limit untuk class metadata
# -XX:+HeapDumpOnOutOfMemoryError : Create dump file saat OOM untuk debugging
# -Dfile.encoding=UTF-8 : Force UTF-8 encoding
# -XX:+UseG1GC       : Use G1 Garbage Collector (lebih efisien)

# NOTE: Jika RAM < 8GB, turunkan ke:
# org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ⚡ GRADLE PERFORMANCE OPTIMIZATION
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Mempercepat build time

# Parallel build - jalankan tasks secara parallel
org.gradle.parallel=true

# Build cache - reuse output dari build sebelumnya
org.gradle.caching=true

# Configure on demand - hanya configure project yang dibutuhkan
org.gradle.configureondemand=true

# Daemon - keep Gradle process running untuk build berikutnya
org.gradle.daemon=true

# Worker threads - sesuaikan dengan CPU cores (max 4 untuk stability)
org.gradle.workers.max=4

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📱 ANDROID CONFIGURATION
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# AndroidX - WAJIB untuk Flutter modern
android.useAndroidX=true

# ⭐ CRITICAL: DISABLE JETIFIER
# Jetifier converts old support libraries to AndroidX
# For Flutter projects built with AndroidX, this is NOT needed
# Disabling this SIGNIFICANTLY speeds up build time
android.enableJetifier=false

# NOTE: Jika ada dependency yang masih pakai support library (error),
# enable kembali: android.enableJetifier=true

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔧 KOTLIN CONFIGURATION
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

kotlin.code.style=official

# Incremental compilation untuk Kotlin (faster builds)
kotlin.incremental=true
kotlin.incremental.java=true
kotlin.incremental.js=true

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🚀 R8 CODE SHRINKING & OBFUSCATION
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# R8 menggantikan ProGuard - lebih cepat dan lebih efektif

# Enable R8
android.enableR8=true

# Full mode - aggressive optimization (production)
android.enableR8.fullMode=true

# NOTE: Pastikan proguard-rules.pro sudah benar agar notifikasi tidak di-strip!

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔌 DISABLE UNUSED ANDROID FEATURES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Mempercepat build dengan disable fitur yang tidak dipakai

# Keep BuildConfig - needed by some plugins
android.defaults.buildfeatures.buildconfig=true

# Disable AIDL (Android Interface Definition Language) - tidak dipakai Flutter
android.defaults.buildfeatures.aidl=false

# Disable RenderScript - tidak dipakai Flutter
android.defaults.buildfeatures.renderscript=false

# Disable Resource Values - tidak dipakai Flutter
android.defaults.buildfeatures.resvalues=false

# Disable Shaders - tidak dipakai Flutter
android.defaults.buildfeatures.shaders=false

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🪟 WINDOWS-SPECIFIC FIXES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# File System Watcher - disable untuk Windows (avoid file locking issues)
org.gradle.vfs.watch=false

# Isolated projects - disable untuk avoid FileSystemException
org.gradle.unsafe.isolated-projects=false

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🐛 DEBUGGING & LOGGING
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Warning mode - show deprecation warnings
# Options: all, fail, summary, none
org.gradle.warning.mode=all

# Console output - rich untuk colored output
# Options: auto, plain, rich, verbose
org.gradle.console=auto

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📦 BUILD CACHE CONFIGURATION
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Build cache directory (optional - customize location)
# org.gradle.caching.directory=/path/to/cache

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔐 SECURITY (for CI/CD)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Keystore credentials - JANGAN commit ke Git!
# Uncomment dan isi untuk signing release APK
# ANDROID_KEYSTORE_FILE=keystore.jks
# ANDROID_KEYSTORE_PASSWORD=your_password
# ANDROID_KEY_ALIAS=your_alias
# ANDROID_KEY_PASSWORD=your_key_password

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🎯 FLUTTER-SPECIFIC OPTIMIZATIONS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Disable Gradle metadata (Flutter doesn't need it)
systemProp.org.gradle.internal.publish.checksums.insecure=true

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📝 NOTES & TROUBLESHOOTING
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ERROR: "Insufficient system resources exist to complete the requested service"
# SOLUTION: Increase -Xmx value (line 10)

# ERROR: "Could not resolve all dependencies"
# SOLUTION: Check internet connection, clear gradle cache:
#   - Delete ~/.gradle/caches (Linux/Mac) or C:\Users\YourName\.gradle\caches (Windows)
#   - Run: flutter clean && flutter pub get

# ERROR: "Execution failed for task ':app:processReleaseResources'"
# SOLUTION: 
#   - Enable jetifier: android.enableJetifier=true
#   - Check for conflicting dependencies in build.gradle

# ERROR: Build too slow
# SOLUTION:
#   - Increase org.gradle.jvmargs memory
#   - Enable parallel builds
#   - Use Gradle daemon
#   - Clear build cache if corrupted

# ERROR: "Daemon will be stopped at the end of the build"
# SOLUTION: Check if -Xmx is too high for available RAM

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🎉 END OF FILE
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Location: android/gradle.properties
# Last updated: 2024-01-21
# Version: Production v4.0